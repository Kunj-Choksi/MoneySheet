import {StyleSheet} from 'react-native';

const globalStyles = StyleSheet.create({
    textDanger: {
        color: 'red',
    },
    textSemiBold: {
        fontWeight: '500',
    },
    textBold: {
        fontWeight: '600',
    },
});

export default globalStyles;
